{
  "name": "@vibemosphere/server",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "predev": "npm run build --prefix ../shared",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -b"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@vibemosphere/shared": "*",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/node": "^20.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
